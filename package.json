{
  "name": "identical-sequence",
  "version": "1.0.0",
  "description": "Find all sequences of identical elements in an array",
  "main": "index.js",
  "scripts": {
    "test": "standard && tape ./test/*.spec.js --tap | tap-spec",
    "coverage": "nyc --reporter=lcov npm test",
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [
    "identical",
    "sequence",
    "equivalent",
    "row",
    "array",
    "element"
  ],
  "author": "Aram Drevekenin",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.11.12",
    "standard": "^7.1.2",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
