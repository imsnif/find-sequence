{
  "name": "find-sequence",
  "version": "1.0.3",
  "description": "Find all sequences of elements in an array",
  "main": "index.js",
  "scripts": {
    "test": "standard && tape ./test/*.spec.js --tap | tap-spec",
    "coverage": "nyc --reporter=lcov npm test",
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [
    "sequence",
    "iterator",
    "equivalent",
    "row",
    "array",
    "element"
  ],
  "author": "Aram Drevekenin",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.11.12",
    "nyc": "^8.1.0",
    "standard": "^8.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/imsnif/find-sequence"
  }
}
